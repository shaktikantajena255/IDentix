[phases.setup]
nixPkgs = [
  "tesseract",
  "tesseract-lang",
  "libGL",
  "libglib2.0",
  "python312",
]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
